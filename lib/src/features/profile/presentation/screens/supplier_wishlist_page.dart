import 'package:flutter/material.dart';

class SupplierWishlistPage extends StatelessWidget {
  const SupplierWishlistPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Wishlist"),
        centerTitle: true,
      ),
      body: const Center(
        child: Text("Wishlist Feature Coming Soon"),
      ),
    );
  }
}